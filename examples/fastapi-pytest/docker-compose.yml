services:
  postgres:
    image: ghcr.io/taktekhq/petri-postgres:16.4-0.1-alpha
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

  app:
    image: python:3.12-slim
    working_dir: /app
    volumes: [".:/app"]
    depends_on: [postgres]
